<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- USB connection detection -->
    <uses-feature android:name="android.hardware.usb.accessory" android:required="false"/>
    <uses-permission android:name="android.permission.USB_PERMISSION"/>

    <!-- Network (TCP socket over ADB) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Keep CPU on during streaming -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Reverse remote: capture this screen and stream it to the Mac -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Client keep-alive: hold the process at foreground priority while the
         extended-screen link is up, so Android won't freeze/kill it (and drop
         the socket + decoder) when backgrounded. connectedDevice models the
         USB-connected Mac and has no daily runtime cap; CHANGE_NETWORK_STATE
         satisfies its runtime prerequisite. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>

    <application
        android:label="Extended Screen"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="landscape"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- Reverse-remote screen capture (MediaProjection) foreground service -->
        <service
            android:name=".plugins.ScreenCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection"/>

        <!-- Client keep-alive foreground service (extended-screen decode link) -->
        <service
            android:name=".plugins.ClientKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
